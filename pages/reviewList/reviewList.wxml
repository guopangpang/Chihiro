<view class="review_list_container">
    <review-header reviewHeader="{{review_header}}"></review-header>
    <review-item wx:for="{{reviewList}}" wx:key="{{index}}" reviewItem="{{item}}"></review-item>
</view>
